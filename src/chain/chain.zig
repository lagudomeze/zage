//! Chain abstraction — internal implementation detail.
//!
//! Chain is NOT part of Zage's public API. The primary user-facing abstraction
//! is the Agent Loop (ReAct cycle) in `src/agent/loop.zig`.
//!
//! Chains exist internally for composing sequential processing steps, but
//! users should interact with the framework through Agent + Harness.

test {}
