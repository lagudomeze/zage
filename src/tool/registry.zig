//! Tool registry — manages the set of available tools.
//!
//! Handles tool registration, lookup by name, and JSON schema generation
//! for LLM function-calling. Implements the Tool interface validation from
//! `src/core/interfaces.zig`.
//!
//! This module will be fleshed out in Phase 1.

test {}
